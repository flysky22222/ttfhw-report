# TTHFW 用户场景验证报告

## 一、概述

- 目标：`openGauss 7.0.0-RC3 Docker 极简版 (x86_64)`
- 时间：2026-05-07T09:00:00 ~ 2026-05-07T09:10:00（600 秒）
- 镜像：`` —— 历史 md 报告未结构化记录镜像

## 二、环境

- 容器 OS：｜架构：｜Python：

## 三、文档阅读 / 抽取

- **test_goal**：作为 openGauss 用户，按 openGauss 官网最新版本（7.0.0-RC3）的安装文档（架构 x86，操作系统 openEuler 24.03 LTS）安装网页上的极简版 Docker 镜像，完成下载、安装、使用，发现安装文档缺陷。　（来源：测试目标）
- **doc_completeness**：官方文档安装流程基本可用，但存在镜像名不一致等阻断性缺陷，需用户自行排查。　（来源：1.2 测试总结）

## 四、四阶段 / 结果

| 阶段 | 状态 | 耗时(s) | 断点 |
|------|------|--------|------|
| 了解 | 成功 | 300 | 0 |
| 安装 | 成功 | 120 | 1 |
| 使用 | 成功 | 60 | 0 |
| 贡献 | 断点 | 120 | 1 |

## 五、文档缺陷清单

- Docker 镜像名与文档示例不一致（严重）
- 文档示例下载 URL 过期（中等）
- Docker 镜像基础 OS 版本未明确说明（建议）
- 镜像名不一致、示例URL过期、OS版本未说明

## 七、结论

成功。发现镜像名不一致等3处文档缺陷，使用 `docker images` 查看实际镜像名后修正，验证全部通过。

> 本 md 由 `tools/ttfhw/cli/render_report.py` 从标准 JSON 报告渲染，JSON 为唯一真值源。