# TTHFW 用户场景验证报告

## 一、概述

- 目标：`openGauss 7.0.0-RC3 极简版 Docker 镜像 (x86_64)`
- 时间：2026-05-07T09:00:00 ~ 2026-05-07T09:15:00（900 秒）
- 镜像：`` —— 历史 md 报告未结构化记录镜像

## 二、环境

- 容器 OS：｜架构：｜Python：

## 三、文档阅读 / 抽取

- **test_goal**：作为 openGauss 用户，按 openGauss 官网最新版本（7.0.0-RC3）的安装文档（架构 x86，操作系统 openEuler 24.03 LTS）安装网页上的极简版 Docker 镜像，完成下载、安装、使用，发现安装文档缺陷。　（来源：测试目标）
- **doc_completeness**：官方文档框架完整，但示例 URL、镜像名、版本信息多处与实际情况不一致。　（来源：1.2 测试总结）

## 四、四阶段 / 结果

| 阶段 | 状态 | 耗时(s) | 断点 |
|------|------|--------|------|
| 了解 | 成功 | 360 | 0 |
| 安装 | 成功 | 480 | 1 |
| 使用 | 成功 | 60 | 0 |
| 贡献 | 部分成功 | 0 | 0 |

## 五、文档缺陷清单

- 文档示例 URL 与实际下载页面 URL 不一致（严重）
- 文档顶部版本选择与下载页面版本类型不对应（中等）
- Docker 版下载页面未明确标注操作系统版本（中等）
- 镜像名与文档示例不一致（严重）
- 文档示例 URL 域名已失效或仅用于测试（严重）
- 容器内 OS 版本与测试目标不一致（中等）
- 严重缺陷 2 处（URL域名失效、镜像名不一致）
- 中等缺陷 4 处（版本选择不对应、OS版本标注缺失、版本信息不一致）
- URL域名失效、镜像名不一致、OS版本标注缺失、文档示例过时

## 七、结论

成功。数据库安装并验证成功，但发现 6 处文档缺陷，需要用户自行调整命令参数。

> 本 md 由 `tools/ttfhw/cli/render_report.py` 从标准 JSON 报告渲染，JSON 为唯一真值源。